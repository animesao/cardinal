# Template for local development only.
# Copy this file to `.env` (which is git-ignored) and replace placeholders.
# Never commit `.env` with real credentials.

MYSQL_ROOT_PASSWORD=change_me_in_local_env_only
MYSQL_DATABASE=myapp
MYSQL_USER=bot
MYSQL_PASSWORD=change_me_in_local_env_only
